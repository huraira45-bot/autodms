# cmd can misread labels (call :name / goto) in a batch file with LF line
# endings, so check this one out with CRLF whatever git's settings are.
Software/scripts/start-after-reboot.cmd text eol=crlf
