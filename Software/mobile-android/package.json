{
  "name": "dealerdesk-service-android",
  "private": true,
  "version": "0.1.0",
  "description": "Android shell for the DealerDesk service advisor tablet app. Bundles the built frontend (../frontend/dist).",
  "scripts": {
    "build:web": "npm --prefix ../frontend run build",
    "sync": "npm run build:web && npx cap sync android",
    "apk:debug": "npm run sync && cd android && gradlew.bat assembleDebug",
    "doctor": "npx cap doctor"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  },
  "engines": {
    "node": ">=22"
  }
}
